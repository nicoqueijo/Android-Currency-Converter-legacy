<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="com.nicoqueijo.android.currencyconverter.helpers.Utility" />

        <variable
            name="currency"
            type="com.nicoqueijo.android.currencyconverter.models.Currency" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="?android:colorBackground"
        android:clickable="@{!currency.selected}"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:orientation="horizontal">

            <android.support.v7.widget.CardView
                android:layout_width="50dp"
                android:layout_height="37dp"
                android:layout_gravity="center_vertical"
                app:cardCornerRadius="2dp">

                <ImageView
                    android:id="@+id/flag"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="fitXY"
                    android:src="@{Utility.getDrawableResourceByName(currency.currencyCode.toLowerCase(), context)}"
                    tools:src="@drawable/usdusd" />

            </android.support.v7.widget.CardView>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingLeft="16dp">

                <TextView
                    android:id="@+id/currency_code"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:text="@{currency.trimmedCurrencyCode}"
                    android:textColor="?attr/text_color"
                    android:textSize="18sp"
                    tools:text="USD" />

                <TextView
                    android:id="@+id/currency_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{Utility.getStringResourceByName(currency.currencyCode, context)}"
                    android:textColor="?attr/text_color"
                    android:textSize="16sp"
                    tools:text="United States Dollar" />

            </LinearLayout>

        </LinearLayout>

        <ImageView
            android:id="@+id/check_mark"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:layout_marginLeft="16dp"
            android:src="@drawable/ic_check"
            android:visibility="@{currency.selected ? View.VISIBLE : View.INVISIBLE}" />

    </LinearLayout>


</layout>
